<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-600 rounded-md outline-none w-full" type="text"
                    placeholder="Title of File" name="title" required>
                <textarea class="block mt-2 px-5 py-2 bg-zinc-600 rounded-md outline-none w-full resize-none"
                    placeholder="Task details here.." name="details"></textarea>
                <input type="submit" class="mt-2 bg-blue-300 px-3 py-2 rounded-md" value="Create task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task  w-72 px-3 py-4 bg-zinc-600 rounded-md">
                        <h1 class="text-white text-xl tracking-tighter"><%= val %></h1>
                        <div class="flex justify-between items-center mt-3">
                            <a href="/files/<%= val %>" class="text-blue-300 inline-block" title="Open File">Read more ></a>
                            <a href="/edit/<%= val %>" title="Edit">📝</a>
                            <form action="/delete" method="post"  class="inline-block">
                                <input type="hidden" name="filename" value="<%= val %>">
                                <button type="submit" class="text-red-500"title="Delete">🗑️</button>
                            </form>
                        </div>

                        
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">No task yet!</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>